import 'package:flutter/material.dart';
import 'drawer_page_template.dart';

class ValidationRequestPage extends StatelessWidget {
  const ValidationRequestPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const DrawerPageTemplate(title: 'Validation Request', icon: Icons.photo_library);
  }
}
